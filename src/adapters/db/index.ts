// src/adapters/db — implementazione di CachePort su Dexie (unico schema IndexedDB — AD-15).
// Può importare da src/app (porte) e src/domain (tipi); mai da ui o altri adapter (AD-7).
// Stub in Story 1.1; lo schema reale arriva in Epic 2/3.

export {};
