// src/adapters/drive — implementazione di StoragePort su Google Drive API (scope drive.file).
// Può importare da src/app (porte) e src/domain (tipi); mai da ui o altri adapter (AD-7).
// Stub in Story 1.1; l'implementazione reale arriva con l'accesso e Drive (Story 1.3/1.4).

export {};
