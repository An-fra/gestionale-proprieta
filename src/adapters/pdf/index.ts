// src/adapters/pdf — implementazione di PdfPort: generazione Ricevute PDF lato client.
// Può importare da src/app (porte) e src/domain (tipi); mai da ui o altri adapter (AD-7).
// Stub in Story 1.1; l'implementazione reale arriva in Epic 4 (Story 4.5).

export {};
