// src/adapters/media — implementazione di MediaPort: fotocamera e compressione immagini.
// Può importare da src/app (porte) e src/domain (tipi); mai da ui o altri adapter (AD-7).
// Stub in Story 1.1; l'implementazione reale arriva in Epic 3 (Story 3.1).

export {};
